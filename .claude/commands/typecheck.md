Run TypeScript type checking across all packages.

This verifies type safety without building the project.

Faster than a full build when you only want to check for type errors.

All type errors should be fixed before committing.
