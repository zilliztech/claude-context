Run ESLint across the codebase.

Lint all packages or specific ones, with optional auto-fix.

Examples:
- Lint all files: "lint"
- Lint and fix: "lint --fix"

The project uses ESLint with TypeScript plugin. All code should pass linting before committing.
