Clean all build artifacts from the project.

This removes:
- dist/ directories
- node_modules/.cache
- Build outputs
- Temporary files

Use this when you need a fresh build or are troubleshooting build issues.

After cleaning, you may need to run "pnpm build" to rebuild the project.
