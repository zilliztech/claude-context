Run tests for the project.

Run all tests or specify a specific package to test.

Examples:
- Run all tests: "test" or "test all"
- Run core package tests: "test core"
- Run with coverage: "test --coverage"

The project uses Jest for testing. Tests should be run before committing changes.
