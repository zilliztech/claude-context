Start development mode with file watching.

Run development mode for all packages or a specific package.

Examples:
- Dev mode all packages: "dev"
- Dev mode for core: "dev core"
- Dev mode for VSCode extension: "dev vscode"

This starts the TypeScript compiler in watch mode and rebuilds on file changes.
